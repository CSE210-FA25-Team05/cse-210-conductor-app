# Root-level env used by docker-compose only (no secrets)

# Database URL used inside Docker containers
DOCKER_DATABASE_URL=postgresql://conductor:conductor@db:5432/conductor_db

# Frontend URL (for CORS / backend config)
FRONTEND_URL=http://localhost:5173

# Node environment
NODE_ENV=development
